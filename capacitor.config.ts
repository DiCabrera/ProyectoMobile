import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.tregistrapp.app',
  appName: 'ResgistrAppDuocUC',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
